-- Install cron
sudo apt-get update
sudo apt-get install cron


-- to run cron
crontab -e

-- Pick up your favorite editor ( I prefer VIM)


-- To run Daily Automatically
0 20 * * * /path/to/myscript.sh

-- The syntax for a cron job is as follows:
minute (0 - 59) hour (0 - 23) day of the month (1 - 31) month (1 - 12) day of the week (0 - 6) command to be executed
